const basePath = '../assets';
const iconsPath = basePath + '/icons';
const imagesPath = basePath + '/images';

export default {
    // Icons
    socials_fb: require(`${iconsPath}/socials_fb.png`),

    // Images
    wishlist_bcg_image: require(`${imagesPath}/wishlist_bcg_image.png`),
};
